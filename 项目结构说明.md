# 项目结构说明

```
QFNULibraryBook-main/
│
├── 一键启动.bat                    # 一键启动前后端服务
├── 启动后端服务.bat                # 单独启动后端
├── 启动前端界面.bat                # 单独启动前端
│
├── README.md                       # 原项目说明
├── README_V2.md                    # v2.0 完整说明文档 ⭐
│
├── backend/                        # 后端服务目录
│   ├── app.py                     # Flask 主程序 ⭐
│   ├── requirements.txt           # Python 依赖包
│   ├── library.db                 # SQLite 数据库（自动生成）
│   │
│   ├── database/                  # 数据库模块
│   │   └── db.py                 # 数据库初始化
│   │
│   ├── models/                    # 数据模型
│   │   └── account.py            # 账号和预约记录模型
│   │
│   ├── routes/                    # API 路由
│   │   ├── account_routes.py    # 账号管理 API
│   │   └── reservation_routes.py # 预约管理 API
│   │
│   └── utils/                     # 工具函数
│       ├── token_utils.py        # Token 解析和验证
│       └── library_api.py        # 图书馆 API 封装
│
├── frontend/                       # 前端界面目录
│   ├── index.html                 # 主页面 ⭐
│   ├── css/
│   │   └── style.css             # 样式表（含响应式设计）
│   └── js/
│       └── app.js                # 应用逻辑（含 GSAP 动画）
│
├── py/                            # 原项目 Python 脚本（保留）
├── web/                           # 原项目 Web 文件（保留）
├── json/                          # 座位信息 JSON（保留）
└── v3.1/                          # 历史版本（保留）
```

## 核心文件说明

### 后端核心

| 文件 | 说明 | 关键功能 |
|------|------|----------|
| `backend/app.py` | Flask 主程序 | 应用初始化、路由注册、CORS 配置 |
| `backend/models/account.py` | 数据模型 | 账号表、预约记录表定义 |
| `backend/routes/account_routes.py` | 账号管理 API | 增删改查账号 |
| `backend/routes/reservation_routes.py` | 预约管理 API | 测试、执行、签到、取消 |
| `backend/utils/token_utils.py` | Token 工具 | JWT 解析、验证、过期检测 |
| `backend/utils/library_api.py` | 图书馆 API | 座位查询、预约提交、AES 加密 |

### 前端核心

| 文件 | 说明 | 关键功能 |
|------|------|----------|
| `frontend/index.html` | 主页面 | 结构布局、表单、账号列表 |
| `frontend/css/style.css` | 样式表 | 渐变背景、卡片、动画、响应式 |
| `frontend/js/app.js` | 应用逻辑 | API 请求、GSAP 动画、状态管理 |

## 数据流转

```
用户操作（前端）
    ↓
Fetch API 请求
    ↓
Flask 路由（backend/routes/）
    ↓
业务逻辑处理
    ↓
├─ 数据库操作（SQLAlchemy）
└─ 图书馆 API 调用（utils/library_api.py）
    ↓
返回 JSON 响应
    ↓
前端渲染 + GSAP 动画
```

## 技术栈对照

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| 前端框架 | 原生 HTML/CSS/JS | - |
| 动画库 | GSAP | 3.12.5 |
| HTTP 请求 | Fetch API | ES6 |
| 后端框架 | Flask | 3.0.0 |
| 数据库 | SQLite | 3.x |
| ORM | Flask-SQLAlchemy | 3.1.1 |
| 跨域 | Flask-CORS | 4.0.0 |
| 加密 | Cryptography | 41.0.7 |

## 端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | 5000 | Flask 开发服务器 |
| 前端界面 | 8080 | Python http.server |

## 启动顺序

1. **后端优先**：先启动 Flask 服务（端口 5000）
2. **前端其次**：再启动前端服务（端口 8080）
3. **浏览器访问**：http://127.0.0.1:8080

## 与原项目的兼容性

- ✅ **保留原脚本**：`py/` 目录下的所有原始脚本保持不变
- ✅ **保留原配置**：`py/config.json` 等配置文件继续可用
- ✅ **共存运行**：新旧版本可以同时存在，互不影响
- ✅ **数据独立**：v2.0 使用独立的 SQLite 数据库

## 开发建议

### 修改后端逻辑
1. 编辑 `backend/routes/*.py`
2. Flask 会自动重载（debug=True）
3. 无需重启，刷新浏览器即可

### 修改前端样式
1. 编辑 `frontend/css/style.css`
2. 浏览器 Ctrl+Shift+R 强制刷新
3. 或 F12 → Network → Disable cache

### 修改前端逻辑
1. 编辑 `frontend/js/app.js`
2. 浏览器 F5 刷新
3. F12 Console 查看日志

### 数据库查看
使用 [DB Browser for SQLite](https://sqlitebrowser.org/) 打开 `backend/library.db`

## 常见问题

### Q: 后端启动失败？
**A**: 检查 Python 版本（需 3.7+）和依赖安装：
```bash
cd backend
pip install -r requirements.txt
```

### Q: 前端无法连接后端？
**A**: 确认后端服务已启动且端口正确（5000），检查 `frontend/js/app.js` 中 `API_BASE` 配置。

### Q: Token 总是显示过期？
**A**: Token 有效期约 100 分钟，需在浏览器重新登录获取新 Token。

### Q: 数据库在哪里？
**A**: `backend/library.db`，首次运行自动创建。

## 部署建议（生产环境）

### 后端
- 使用 **Gunicorn** 或 **uWSGI** 代替 Flask 开发服务器
- 配置 **Nginx** 反向代理
- 数据库迁移到 **MySQL/PostgreSQL**

### 前端
- 使用 **Nginx** 直接托管静态文件
- 启用 **Gzip** 压缩
- 配置 **CDN** 加速

### 示例 Nginx 配置
```nginx
server {
    listen 80;
    server_name library.example.com;

    # 前端静态文件
    location / {
        root /path/to/frontend;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

**文档版本**: 1.0  
**更新时间**: 2026-07-05
