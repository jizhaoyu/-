# 项目结构说明

```
QFNULibraryBook-main/
│
├── 启动后端服务.bat / .sh          # 启动 Flask 后端
├── 启动Vue3前端.bat / .sh          # 启动 Vue3 前端
│
├── README.md                       # 项目完整说明
├── 快速启动指南.md                 # 快速上手指南
├── API文档.md                      # 后端 API 文档
│
├── backend/                        # 后端服务目录
│   ├── app.py                     # Flask 主程序 ⭐
│   ├── requirements.txt           # Python 依赖包
│   ├── library.db                 # SQLite 数据库（自动生成）
│   │
│   ├── database/                  # 数据库模块
│   │   └── db.py                 # 数据库初始化
│   │
│   ├── models/                    # 数据模型
│   │   └── account.py            # 账号和预约记录模型
│   │
│   ├── routes/                    # API 路由
│   │   ├── account_routes.py    # 账号管理 API
│   │   └── reservation_routes.py # 预约管理 API
│   │
│   └── utils/                     # 工具函数
│       ├── token_utils.py        # Token 解析和验证
│       └── library_api.py        # 图书馆 API 封装
│
├── frontend-vue/                   # Vue3 前端目录
│   ├── index.html                 # HTML 入口
│   ├── package.json               # 依赖与脚本
│   ├── vite.config.ts             # Vite 配置（端口、/api 代理）
│   ├── tsconfig.json              # TypeScript 配置
│   └── src/
│       ├── main.ts               # 应用入口
│       ├── App.vue               # 根组件
│       ├── api/                  # API 封装（axios）
│       ├── components/           # 组件（账号表单、列表、详情）
│       ├── router/               # 路由
│       ├── stores/               # Pinia 状态管理
│       ├── styles/               # 全局样式
│       └── views/                # 页面视图
│
├── json/                          # 座位信息 JSON 数据
│   └── seat_info/                # 各自习室座位配置
│
└── assets/                        # 资源文件（截图等）
```

## 核心文件说明

### 后端核心

| 文件 | 说明 | 关键功能 |
|------|------|----------|
| `backend/app.py` | Flask 主程序 | 应用初始化、路由注册、CORS 配置 |
| `backend/models/account.py` | 数据模型 | 账号表、预约记录表定义 |
| `backend/routes/account_routes.py` | 账号管理 API | 增删改查账号 |
| `backend/routes/reservation_routes.py` | 预约管理 API | 测试、执行、签到、取消 |
| `backend/utils/token_utils.py` | Token 工具 | JWT 解析、验证、过期检测 |
| `backend/utils/library_api.py` | 图书馆 API | 座位查询、预约提交、AES 加密 |

### 前端核心

| 文件 | 说明 | 关键功能 |
|------|------|----------|
| `frontend-vue/src/main.ts` | 应用入口 | 挂载 Vue、注册插件 |
| `frontend-vue/src/App.vue` | 根组件 | 页面布局 |
| `frontend-vue/src/api/` | API 封装 | axios 请求、接口定义 |
| `frontend-vue/src/components/` | 组件 | 账号表单、列表、详情面板 |
| `frontend-vue/src/stores/` | 状态管理 | Pinia 账号状态 |
| `frontend-vue/vite.config.ts` | 构建配置 | 端口 8080、`/api` 代理到 5000 |

## 数据流转

```
用户操作（Vue3 前端）
    ↓
axios 请求（经 Vite /api 代理）
    ↓
Flask 路由（backend/routes/）
    ↓
业务逻辑处理
    ↓
├─ 数据库操作（SQLAlchemy）
└─ 图书馆 API 调用（utils/library_api.py）
    ↓
返回 JSON 响应
    ↓
前端渲染 + GSAP 动画
```

## 技术栈对照

| 层级 | 技术选型 | 版本 |
|------|----------|------|
| 前端框架 | Vue 3 | ^3.4.0 |
| 构建工具 | Vite | ^5.0.0 |
| 语言 | TypeScript | ^5.3.0 |
| UI 组件库 | Arco Design Vue | ^2.55.0 |
| 状态管理 | Pinia | ^2.1.7 |
| 路由 | Vue Router | ^4.2.5 |
| HTTP 请求 | axios | ^1.6.0 |
| 动画库 | GSAP | ^3.12.5 |
| 后端框架 | Flask | 3.0.0 |
| 数据库 | SQLite | 3.x |
| ORM | Flask-SQLAlchemy | 3.1.1 |
| 跨域 | Flask-CORS | 4.0.0 |
| 加密 | Cryptography | 41.0.7 |

## 端口分配

| 服务 | 端口 | 说明 |
|------|------|------|
| 后端 API | 5000 | Flask 开发服务器 |
| 前端界面 | 8080 | Vite 开发服务器（含 /api 代理） |

## 启动顺序

1. **后端优先**：先启动 Flask 服务（端口 5000）
2. **前端其次**：再启动 Vue3 前端（端口 8080）
3. **浏览器访问**：http://127.0.0.1:8080

## 开发建议

### 修改后端逻辑
1. 编辑 `backend/routes/*.py`
2. Flask 会自动重载（debug=True）
3. 无需重启，刷新浏览器即可

### 修改前端
1. 编辑 `frontend-vue/src/` 下的组件或逻辑
2. Vite 热更新（HMR）自动生效，无需刷新

### 数据库查看
使用 [DB Browser for SQLite](https://sqlitebrowser.org/) 打开 `backend/library.db`

## 常见问题

### Q: 后端启动失败？
**A**: 检查 Python 版本（需 3.7+）和依赖安装：
```bash
cd backend
pip install -r requirements.txt
```

### Q: 前端无法连接后端？
**A**: 确认后端服务已启动且端口正确（5000）。前端通过 `vite.config.ts` 中的 `/api` 代理访问后端，若修改了后端端口需同步更新代理配置。

### Q: Token 总是显示过期？
**A**: Token 有效期约 100 分钟，需在浏览器重新登录获取新 Token。

### Q: 数据库在哪里？
**A**: `backend/library.db`，首次运行自动创建。

## 部署建议（生产环境）

### 后端
- 使用 **Gunicorn** 或 **uWSGI** 代替 Flask 开发服务器
- 配置 **Nginx** 反向代理
- 数据库迁移到 **MySQL/PostgreSQL**

### 前端
- 执行 `npm run build` 生成静态文件（默认输出 `dist/`）
- 使用 **Nginx** 托管构建产物
- 启用 **Gzip** 压缩，配置 **CDN** 加速

### 示例 Nginx 配置
```nginx
server {
    listen 80;
    server_name library.example.com;

    # 前端静态文件（Vite 构建产物）
    location / {
        root /path/to/frontend-vue/dist;
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理
    location /api/ {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
```

---

**文档版本**: 2.0
**更新时间**: 2026-07-06
