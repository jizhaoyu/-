# 项目清理说明

## 📋 清理目的

项目已完成重构，为保持项目结构清晰，建议清理旧版本文件。

## 🗑️ 将被清理的文件

### 旧版本目录
- `py/` - 旧版本 Python 脚本
- `web/` - 旧版本 Web 文件  
- `v3.1/` - 历史版本备份

### 旧版本脚本
- `run_reservation.bat` - 旧的启动脚本
- `启动Web管理.bat` - 旧的 Web 管理脚本

### 旧版本文档
- `README.md` - 旧版本说明（已有 README_V2.md）
- `ID说明书.md` - 旧版本说明文档

## ✅ 保留的文件

### 核心目录
- `backend/` - 新版本后端服务
- `frontend/` - 新版本原生前端
- `frontend-vue/` - 新版本 Vue3 前端
- `json/` - 座位信息数据
- `assets/` - 资源文件

### 新版本脚本
- `一键启动.bat`
- `启动后端服务.bat` / `.sh`
- `启动前端界面.bat` / `.sh`
- `启动Vue3前端.bat` / `.sh`
- `运行自动化测试.bat` / `.sh`
- `清理旧文件.bat` / `.sh`

### 新版本文档
- `README_V2.md` - 新版本完整说明
- `快速启动指南.md`
- `项目结构说明.md`
- `API文档.md`
- `项目总览.md`
- `项目完成总结.md`
- `测试文档.md`
- `测试报告.md`
- 等...

## 🚀 如何清理

### Windows
```bash
# 双击运行
清理旧文件.bat

# 输入 YES 确认
```

### Linux/macOS
```bash
bash 清理旧文件.sh

# 输入 YES 确认
```

## 📁 清理后的结构

旧文件将被移动到 `_archive/old_versions/` 目录，而不是删除：

```
QFNULibraryBook-main/
├── _archive/
│   └── old_versions/        # 旧版本文件归档
│       ├── py/
│       ├── web/
│       ├── v3.1/
│       ├── run_reservation.bat
│       ├── 启动Web管理.bat
│       ├── README_old.md
│       └── ID说明书.md
│
├── backend/                 # 新版本后端 ✓
├── frontend/                # 新版本原生前端 ✓
├── frontend-vue/            # 新版本 Vue3 前端 ✓
├── json/                    # 座位数据 ✓
├── assets/                  # 资源文件 ✓
│
├── 一键启动.bat             # 新版本脚本 ✓
├── 启动后端服务.bat
├── 启动前端界面.bat
├── 启动Vue3前端.bat
├── 运行自动化测试.bat
│
├── README_V2.md            # 新版本文档 ✓
├── 快速启动指南.md
├── 项目结构说明.md
├── API文档.md
└── ...
```

## ⚠️ 注意事项

### 安全性
1. **文件不会被删除**：所有旧文件只是移动到 `_archive/` 目录
2. **可以恢复**：如需恢复，从 `_archive/old_versions/` 移回即可
3. **需要确认**：脚本会要求输入 `YES` 确认

### 何时清理
- ✅ 已验证新版本功能正常
- ✅ 已备份重要数据
- ✅ 不再需要旧版本功能

### 不建议清理的情况
- ❌ 正在使用旧版本脚本
- ❌ 需要对比新旧版本
- ❌ 未完成功能测试

## 🔄 如何恢复

如果清理后需要恢复旧文件：

```bash
# Windows
move _archive\old_versions\py py
move _archive\old_versions\web web

# Linux/macOS
mv _archive/old_versions/py .
mv _archive/old_versions/web .
```

## 📊 清理效果

### 清理前
```
总文件数: 200+
项目目录: 混合新旧版本
代码重复: 存在
```

### 清理后
```
总文件数: 104 (核心文件)
项目目录: 仅保留新版本
代码重复: 无
结构清晰度: ⭐⭐⭐⭐⭐
```

## ✅ 推荐操作

1. **运行测试**：确认新版本功能正常
   ```bash
   运行自动化测试.bat
   ```

2. **备份（可选）**：如需完整备份
   ```bash
   # 将整个项目复制一份
   ```

3. **执行清理**：运行清理脚本
   ```bash
   清理旧文件.bat
   ```

4. **验证结果**：检查项目结构
   ```bash
   dir /b    # Windows
   ls -la    # Linux/macOS
   ```

## 📝 清理清单

- [ ] 已运行自动化测试
- [ ] 已验证新版本功能
- [ ] 已备份重要数据（如需要）
- [ ] 已阅读清理说明
- [ ] 准备执行清理

---

**提示**: 清理是可选的，旧文件不会影响新版本的使用。如果不确定，可以先不清理。
