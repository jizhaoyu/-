# 快速启动指南 ⚡

> 3 分钟上手图书馆预约管理系统 v2.0

## 🎯 一键启动（推荐）

1. **双击运行** `一键启动.bat`
2. **等待** 3-5 秒（自动安装依赖）
3. **访问** http://127.0.0.1:8080

完成！🎉

---

## 📝 第一次使用

### 步骤 1: 获取 Token

1. 打开图书馆网站：http://libyy.qfnu.edu.cn/h5/index.html
2. 完成登录（滑动验证码）
3. 按 **F12** 打开控制台
4. 输入并执行：
   ```javascript
   localStorage.getItem('token')
   ```
5. 复制输出的 Token（一长串字符）

### 步骤 2: 添加账号

1. 在左侧表单填写：
   - **姓名**：你的名字（如：张三）
   - **Token**：刚才复制的 Token
   - **区域 ID**：22（默认）
   - **预约日期**：明天（默认）
   - **座位策略**：第一个空闲（默认）

2. 点击 **"➕ 添加账号"**

### 步骤 3: 测试预约

1. 点击右侧列表中的账号卡片
2. 在详情面板点击 **"🧪 测试"**
3. 查看是否有可用座位

### 步骤 4: 执行预约

1. 确认测试成功后
2. 点击 **"🎯 预约"**
3. 确认弹窗
4. 等待预约结果

---

## 🔧 手动启动（高级）

### 后端服务

```bash
# 进入后端目录
cd backend

# 创建虚拟环境（仅首次）
python -m venv .venv

# 激活虚拟环境
.venv\Scripts\activate        # Windows
source .venv/bin/activate     # macOS/Linux

# 安装依赖（仅首次）
pip install -r requirements.txt

# 启动服务
python app.py
```

### 前端界面

```bash
# 进入前端目录
cd frontend

# 启动 HTTP 服务
python -m http.server 8080
```

---

## ❓ 常见问题

### ⚠️ 提示"连接后端失败"

**原因**：后端服务未启动  
**解决**：先运行 `启动后端服务.bat`，等待启动完成后再访问前端

### ⚠️ Token 显示"已过期"

**原因**：Token 有效期约 100 分钟  
**解决**：重新登录图书馆网站获取新 Token，更新账号

### ⚠️ 预约失败

**可能原因**：
1. 没有空闲座位
2. 已有预约记录
3. Token 过期
4. 网络问题

**解决**：
1. 换个时间段试试
2. 先取消现有预约
3. 更新 Token
4. 检查网络连接

### ⚠️ 端口被占用

**现象**：启动时提示端口已被使用  
**解决**：
```bash
# Windows 查看端口占用
netstat -ano | findstr :5000
netstat -ano | findstr :8080

# 关闭占用进程
taskkill /PID <进程ID> /F
```

---

## 📱 功能速查

| 功能 | 位置 | 说明 |
|------|------|------|
| 添加账号 | 左侧表单 | 填写姓名和 Token |
| 查看详情 | 右侧列表 | 点击账号卡片 |
| 编辑账号 | 详情面板 | "✏️ 编辑"按钮 |
| 测试预约 | 详情面板 | "🧪 测试"按钮 |
| 执行预约 | 详情面板 | "🎯 预约"按钮 |
| 签到 | 详情面板 | "📍 签到"按钮 |
| 删除账号 | 详情面板 | "🗑️ 删除"按钮 |
| 刷新列表 | 右上角 | "🔄"按钮 |

---

## 🎨 界面说明

```
┌─────────────────────────────────────────────────┐
│  📚 图书馆预约管理系统                          │
│  多账号管理 · Token 实时监控 · 一键预约        │
└─────────────────────────────────────────────────┘

┌──────────────────┐  ┌─────────────────────────┐
│  添加/编辑账号   │  │  账号列表            🔄 │
│                  │  │                         │
│  姓名: _______   │  │  ┌───────────────────┐ │
│  Token: ______   │  │  │ 张三              │ │
│  区域 ID: 22     │  │  │ ✓有效 📍区域22   │ │
│  预约日期: 明天  │  │  └───────────────────┘ │
│  座位策略: 第一  │  │                         │
│                  │  │  🔐 认证信息            │
│  ➕ 添加账号     │  │  姓名: 张三             │
│                  │  │  Token状态: ✓有效      │
└──────────────────┘  │  过期时间: 2026-...    │
                       │  剩余时间: 95 分钟     │
                       │                         │
                       │  ⚙️ 预约配置            │
                       │  区域ID: 22            │
                       │  预约日期: 明天        │
                       │  座位策略: 第一个空闲  │
                       │                         │
                       │  ✏️编辑 🧪测试 🎯预约  │
                       │  📍签到 🗑️删除         │
                       └─────────────────────────┘
```

---

## 🚀 下一步

- 查看完整文档：[README_V2.md](README_V2.md)
- 了解项目结构：[项目结构说明.md](项目结构说明.md)
- API 文档：启动后访问 http://127.0.0.1:5000

---

**需要帮助？** 查看 [README_V2.md](README_V2.md) 获取详细说明  
**遇到问题？** 检查上面的"常见问题"章节

祝使用愉快！📚✨
