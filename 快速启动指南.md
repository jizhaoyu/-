# 快速启动指南 ⚡

> 3 分钟上手图书馆预约管理系统 v2.0（Flask 后端 + Vue3 前端）

## 🎯 启动步骤

系统分前后端两部分，需分别启动。

### 1️⃣ 启动后端服务

双击运行 `启动后端服务.bat`（Windows）或执行 `启动后端服务.sh`（macOS/Linux），脚本会自动创建虚拟环境、安装依赖并启动 Flask 服务。

后端地址：http://127.0.0.1:5000

### 2️⃣ 启动 Vue3 前端

双击运行 `启动Vue3前端.bat`（Windows）或执行 `启动Vue3前端.sh`（macOS/Linux），脚本会自动安装 npm 依赖并启动开发服务器。

前端地址：http://127.0.0.1:8080

### 3️⃣ 打开浏览器

访问 http://127.0.0.1:8080 即可使用。前端已配置 `/api` 代理到后端 5000 端口。

完成！🎉

---

## 📝 第一次使用

### 步骤 1: 获取 Token

1. 打开图书馆网站：http://libyy.qfnu.edu.cn/h5/index.html
2. 完成登录（滑动验证码）
3. 按 **F12** 打开控制台
4. 输入并执行：
   ```javascript
   localStorage.getItem('token')
   localStorage.getItem('name')
   ```
5. 复制输出的 Token（一长串字符）和姓名

### 步骤 2: 添加账号

1. 在表单填写：
   - **姓名**：你的名字（如：张三）
   - **Token**：刚才复制的 Token（支持带 `bearer` 前缀）
   - **区域 ID**：22（默认）
   - **预约日期**：明天（默认）
   - **座位策略**：第一个空闲（默认）

2. 点击 **"➕ 添加账号"**

### 步骤 3: 测试预约

1. 点击账号卡片查看详情
2. 点击 **"🧪 测试"**，查看是否有可用座位

### 步骤 4: 执行预约

1. 确认测试成功后点击 **"🎯 预约"**
2. 确认弹窗，等待预约结果

---

## 🔧 手动启动（高级）

### 后端服务

```bash
cd backend

# 创建虚拟环境（仅首次）
python -m venv .venv

# 激活虚拟环境
.venv\Scripts\activate        # Windows
source .venv/bin/activate     # macOS/Linux

# 安装依赖（仅首次）
pip install -r requirements.txt

# 启动服务
python app.py
```

### Vue3 前端

```bash
cd frontend-vue

# 安装依赖（仅首次）
npm install

# 启动开发服务器
npm run dev
```

---

## ❓ 常见问题

### ⚠️ 提示"连接后端失败"

**原因**：后端服务未启动
**解决**：先运行 `启动后端服务.bat`，等待启动完成后再访问前端

### ⚠️ Token 显示"已过期"

**原因**：Token 有效期约 100 分钟
**解决**：重新登录图书馆网站获取新 Token，更新账号

### ⚠️ 预约失败

**可能原因**：
1. 没有空闲座位
2. 已有预约记录
3. Token 过期
4. 网络问题

**解决**：
1. 换个时间段试试
2. 先取消现有预约
3. 更新 Token
4. 检查网络连接

### ⚠️ 端口被占用

**现象**：启动时提示端口已被使用
**解决**：
```bash
# Windows 查看端口占用
netstat -ano | findstr :5000
netstat -ano | findstr :8080

# 关闭占用进程
taskkill /PID <进程ID> /F
```

### ⚠️ npm install 很慢

使用国内镜像加速：
```bash
npm install --registry=https://registry.npmmirror.com
```

---

## 🚀 下一步

- 查看完整文档：[README.md](README.md)
- 了解项目结构：[项目结构说明.md](项目结构说明.md)
- API 文档：见 [API文档.md](API文档.md)

---

**需要帮助？** 查看 [README.md](README.md) 获取详细说明
**遇到问题？** 检查上面的"常见问题"章节

祝使用愉快！📚✨
