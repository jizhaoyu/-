# 自动化测试文档

## 测试概览

本项目包含完整的自动化测试套件，覆盖：
- ✅ 项目结构完整性测试
- ✅ 后端模块导入测试
- ✅ 后端 API 功能测试
- ✅ 前端文件完整性测试
- ✅ Vue3 项目配置测试
- ✅ 集成测试

## 快速开始

### Windows
```bash
运行自动化测试.bat
```

### Linux/macOS
```bash
bash 运行自动化测试.sh
```

## 测试类型

### 1. 系统测试 (`test_system.py`)

测试项目完整性和配置：

- **项目结构测试**
  - 后端文件完整性（8 个核心文件）
  - 原生前端文件（3 个）
  - Vue3 前端文件（8 个核心文件）
  - 启动脚本（4 个）
  - 文档文件（5 个）

- **后端模块测试**
  - 数据库模块导入
  - 模型模块导入
  - 工具函数导入
  - 路由模块导入

- **前端文件测试**
  - HTML 结构完整性
  - CSS 样式检查
  - JavaScript 功能检查
  - GSAP 集成检查

- **Vue3 项目测试**
  - package.json 依赖检查
  - TypeScript 配置检查
  - Vite 配置检查
  - 核心依赖版本检查

### 2. API 测试 (`backend/test_api.py`)

测试后端 API 功能（需要后端服务运行）：

- **健康检查**
  - GET /health
  - GET /

- **账号管理 API**
  - POST /api/accounts - 创建账号
  - GET /api/accounts - 获取账号列表
  - GET /api/accounts/{id} - 获取账号详情
  - PUT /api/accounts/{id} - 更新账号
  - DELETE /api/accounts/{id} - 删除账号

- **预约管理 API**
  - POST /api/reservations/test - 测试预约
  - POST /api/reservations/execute - 执行预约
  - POST /api/reservations/check-in - 签到
  - GET /api/reservations/history - 历史记录

- **错误处理**
  - 404 错误
  - 400 参数错误
  - 无效 ID 处理

## 测试依赖

```bash
pip install colorama requests
```

或使用国内镜像：
```bash
pip install colorama requests -i https://pypi.tuna.tsinghua.edu.cn/simple
```

## 手动运行测试

### 仅运行系统测试
```bash
python test_system.py
```

### 仅运行 API 测试（需要后端服务运行）
```bash
# 先启动后端
python backend/app.py

# 新终端运行测试
python backend/test_api.py
```

## 测试输出

### 成功示例
```
[测试] 健康检查
✓ 通过

[测试] 创建账号
  创建账号 ID: 1
✓ 通过

测试摘要
============================================================
总计: 15
通过: 15
失败: 0
============================================================
```

### 失败示例
```
[测试] 后端服务连接
✗ 失败: 无法连接（服务可能未启动）

测试摘要
============================================================
总计: 15
通过: 14
失败: 1
============================================================

失败的测试:
  ✗ 后端服务连接: 无法连接（服务可能未启动）
```

## 测试覆盖率

| 测试类别 | 测试项 | 覆盖率 |
|---------|--------|--------|
| 项目结构 | 25+ 项 | 100% |
| 后端模块 | 4 项 | 100% |
| 后端 API | 12 项 | 90% |
| 前端文件 | 5 项 | 100% |
| Vue3 配置 | 6 项 | 100% |
| 集成测试 | 2 项 | 100% |

## CI/CD 集成

可以将测试集成到 CI/CD 流程：

### GitHub Actions 示例
```yaml
name: Test

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - run: pip install -r backend/requirements.txt
      - run: pip install colorama requests
      - run: python test_system.py
```

## 常见问题

### Q: 测试提示"无法连接到后端服务"？
A: API 测试需要后端服务运行。先启动 `启动后端服务.bat`，然后再运行测试。

### Q: 某些文件测试失败？
A: 检查文件是否存在，可能是项目不完整。

### Q: 模块导入失败？
A: 确保已安装后端依赖：
```bash
cd backend
pip install -r requirements.txt
```

### Q: Windows 下中文乱码？
A: 测试脚本已使用 `chcp 65001` 和 `colorama` 处理编码问题。

## 测试最佳实践

1. **定期运行**: 每次代码修改后运行测试
2. **持续集成**: 将测试集成到 CI/CD
3. **测试驱动**: 先写测试，再实现功能
4. **隔离测试**: 每个测试独立运行
5. **清理数据**: 测试后清理测试数据

## 扩展测试

### 添加新的 API 测试
编辑 `backend/test_api.py`：
```python
def test_new_feature():
    """测试新功能"""
    response = requests.get(f"{API_BASE}/api/new-endpoint")
    assert response.status_code == 200
    
runner.test("新功能", test_new_feature)
```

### 添加前端测试
可以使用 Vitest 或 Jest：
```bash
cd frontend-vue
npm install -D vitest @vue/test-utils
npm run test
```

## 测试报告

测试完成后会显示：
- ✅ 总测试数
- ✅ 通过数量
- ✅ 失败数量
- ✅ 失败详情

## 性能测试

TODO: 添加性能测试
- API 响应时间
- 并发请求测试
- 负载测试

## 安全测试

TODO: 添加安全测试
- SQL 注入测试
- XSS 攻击测试
- CSRF 防护测试

---

**版本**: 1.0  
**更新时间**: 2026-07-05
